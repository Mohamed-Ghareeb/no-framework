{% extends 'layouts/app.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
    <h1>Home</h1>

    <div>
        {% for user in users %}
            <div>{{ user.name }}</div>
        {% endfor %}
    </div>
{% endblock %}