{% extends 'layouts/app.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
    <h1>Home</h1>
    <div>
        {% for user in users %}
            <div><a href="/users/{{ user.id }}">{{ user.first_name }}</a></div>
        {% endfor %}
    </div>
{% endblock %}